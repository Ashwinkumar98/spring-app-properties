spring:
  datasource:
    url: jdbc:mysql://localhost:3306/notes?createDatabaseIfNotExist=true
    schema: notes_schema.sql
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,refresh
  endpoint:
    health:
      show-details: ALWAYS
        
jwt:
  secret: ashwin
  
info:
  app:
    name: Notes Application
    version : 1.0
    java:
      version: 1.8